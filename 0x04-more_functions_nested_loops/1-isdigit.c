#include "main.h"

/**
 * _isupper -prog checks for uppercase charater
 * @c: variable text
 * Return: always 0.
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
