#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void positive_or_negative(int i);
int largest-number(int a, int b, int c);
int convert-day(int month, int day);
void print-remaining-days(int momnth, int day, int year);

#endif
