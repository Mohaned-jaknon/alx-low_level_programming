#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ch;
	char char;

	ch = 'a';
	chara = 'A';
	while
		(ch <= 'z')
			putchar(ch);
	ch++;
	while
		(chara <= 'Z')
			putchar(chara);
	chara++;

	putchar('\n');
	return (0);
}
